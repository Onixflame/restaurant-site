# Local image assets

The current portfolio build references editorial image URLs from Unsplash so the prototype stays lightweight.
For the final portfolio release, replace those URLs with optimized local AVIF/WebP files in this folder and update `src/data/menu.ts` / page hero sources.
