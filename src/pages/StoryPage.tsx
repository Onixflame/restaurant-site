import { PageShell } from '../components/layout/PageShell';
import { PageHero } from '../components/layout/PageHero';
import { effectiveContent, effectiveMusic, effectiveTeam } from '../lib/dataSource';
export function StoryPage(){const team=effectiveTeam(),music=effectiveMusic(),content=effectiveContent();const timeline=content.pages.story.timeline;return <PageShell><PageHero eyebrow={content.pages.story.heroEyebrow} title={content.pages.story.heroTitle} className="story">{content.pages.story.heroText}</PageHero><section className="section paper"><div className="wrap"><div className="section-head reveal"><div><div className="kicker">House of the Black Stone</div><h2 className="display">{content.pages.story.introTitle}</h2></div><p className="copy">{content.pages.story.introText}</p></div><div className="timeline">{timeline.map((chapter,i)=><div className={`timeline-row reveal reveal-delay-${Math.min(i,3)}`} key={`${chapter.year}-${i}`}><div className="timeline-year">{chapter.year}</div><div className="timeline-content"><h3>{chapter.title}</h3><p>{chapter.text}</p></div></div>)}</div></div></section><section className="history-feature reveal"><div className="history-image image-reveal"><img src="https://commons.wikimedia.org/wiki/Special:Redirect/file/Charles_Bridge_at_sunset.jpg?width=1800" alt="Charles Bridge in Prague at sunset"/></div><div className="history-feature-copy"><div className="kicker">What remained</div><h2 className="display">{content.pages.story.featureTitle}</h2><p className="copy">{content.pages.story.featureText}</p><div className="story-quote">“A room should remember what happened before you entered it.”<span>— ONIX house note</span></div></div></section><section className="section" id="team"><div className="wrap"><div className="section-head reveal"><div><div className="kicker">People</div><h2 className="display">{content.pages.story.teamTitle}</h2></div><p className="copy">{content.pages.story.teamText}</p></div><div className="staff-grid">{team.map((x,i)=><article className={`staff-card reveal reveal-delay-${i+1}`} key={x.name}><div className="staff-image"><img src={x.img} alt={x.name}/><div className="staff-overlay"><span>{i===0?'Oak, acidity, restraint':i===1?'Pace before theatre':i===2?'Fruit before sugar':'Czech growers first'}</span></div></div><h3>{x.name}</h3><div className="role">{x.role}</div><p>{x.bio}</p></article>)}</div></div></section><section className="section paper" id="music"><div className="wrap"><div className="section-head reveal"><div><div className="kicker">Salon Sessions</div><h2 className="display">{content.pages.story.musicTitle}</h2></div><p className="copy">{content.pages.story.musicText}</p></div><div className="music-grid">{music.map((x,i)=><article className={`music-row reveal reveal-delay-${i%3}`} key={x.day}><div className="music-day">{x.day}</div><div><h3>{x.title}</h3><p>{x.artist} · {x.note}</p></div><div className="music-time">{x.time}</div></article>)}</div></div></section></PageShell>}
