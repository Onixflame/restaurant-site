import type { DemoSandbox } from '../../types/domain';
export function MusicManager({sandbox,onChange}:{sandbox:DemoSandbox;onChange:(s:DemoSandbox)=>void}){const edit=(i:number,key:'title'|'artist'|'time',value:string)=>{const music=sandbox.music.map((x,n)=>n===i?{...x,[key]:value}:x);onChange({...sandbox,music})};return <section className="admin-section"><h2>Live music programme</h2><div className="admin-edit-list">{sandbox.music.map((m,i)=><div key={m.day}><strong>{m.day}</strong><input value={m.title} onChange={e=>edit(i,'title',e.target.value)}/><input value={m.artist} onChange={e=>edit(i,'artist',e.target.value)}/><input value={m.time} onChange={e=>edit(i,'time',e.target.value)}/></div>)}</div></section>}
export function TeamManager({sandbox,onChange}:{sandbox:DemoSandbox;onChange:(s:DemoSandbox)=>void}){const edit=(i:number,key:'name'|'role'|'bio',value:string)=>{const team=sandbox.team.map((x,n)=>n===i?{...x,[key]:value}:x);onChange({...sandbox,team})};return <section className="admin-section"><h2>Team profiles</h2><div className="admin-edit-list team">{sandbox.team.map((m,i)=><div key={i}><input value={m.name} onChange={e=>edit(i,'name',e.target.value)}/><input value={m.role} onChange={e=>edit(i,'role',e.target.value)}/><textarea rows={3} value={m.bio} onChange={e=>edit(i,'bio',e.target.value)}/></div>)}</div></section>}
